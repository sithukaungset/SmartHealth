<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smarthealth">
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/mainmenu_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mainmenu_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHealth">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.dashboard.Questions"></activity>
        <activity android:name=".ui.dashboard.Disturb"></activity>
        <activity android:name=".ui.dashboard.Memo"></activity>
        <activity android:name=".ui.home.CustomProgressBarActivity"></activity>

    </application>

</manifest>